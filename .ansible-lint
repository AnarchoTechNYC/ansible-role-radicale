# Not sure we care about these quite yet.
warn_list:
   - '106'  # Role name {} does not match ``^[a-z][a-z0-9_]+$`` pattern
   - '208'  # File permissions unset or incorrect
   - '702'  # Tags must contain lowercase letters and digits only
